import SwiftUI

struct AppSidebarView: View {
    var body: some View {
        List {
            Text("RunBot")
                .foregroundStyle(.secondary)
        }
        .navigationTitle("RunBot")
    }
}
